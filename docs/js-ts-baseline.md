# JavaScript / TypeScript Baseline

- TypeScript is used as the primary language.
- ES Modules (ESM) are used across Node and frontend code.
- TS is transpiled or executed via tooling (tsx) in dev.
- Production builds may compile TS to JS where required.
- Known ESM limitations
